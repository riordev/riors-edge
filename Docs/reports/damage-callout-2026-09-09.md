# Player damage callout avoids enemy status rows

The transient red DAMAGE callout now moves above intersecting measured enemy name, health and status bounds. It retains its original position when clear. If the full text cannot fit on screen, only that callout is omitted; the existing damage edge flash remains. This does not change combat damage, status timing or audio.

The original 1280x720 Rot activation frame visibly placed DAMAGE over ROT. Independent review verified matching font measurements, current-frame bounds, strictly upward relocation and viewport handling. The repeated real activation capture shows DAMAGE above SKITTER, with ROT 3.9s and its rail readable below. The larger-nameplate setting was also loaded from the isolated profile and verified visibly: the expanded bars and separated labels remain clear. All three frames from each run were inspected. The first minimal settings file was regenerated by the engine, so that run did not prove larger-nameplate behavior; the follow-up preserved a valid settings version and the requested setting.

Capture directories outside checkout: seat/captures/rot-feedback-1317 (before), rot-callout-1324 (default after), rot-callout-large-1328 (setting not retained), and rot-callout-large-1330 (verified larger setting). These are synthetic presentation fixtures with real status delivery, not earned-build or hearing acceptance. Scheduled frames arrive after Rot expires; only the activation frame witnesses this overlap. Avoidance covers enemy plates, not every independent banner or floating number.

Build and full suite passed with 864 passing, three enumerated expected failures and zero unexpected failures. Status retains its existing out-of-band measurements.
