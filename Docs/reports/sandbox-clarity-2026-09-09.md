# Developer sandbox clarity

Gym-area controls now render only when the existing normalized gym-map predicate is true. Fixed destinations instead explain that regional enemy levels remain authored and direct item-level selection to Gear. The Force Class callback reports the actual resulting class and implemented-kit lookup instead of always warning about kitless classes. Existing class mutation, saves, tuning and grant-level preferences remain unchanged.

All four1280x720 frames inspected: seat/captures/sandbox-coast-1244 and sandbox-gym-1246. Coast shows the fitting explanatory line and no gym increment controls; Gym retains all four increments and its level value. Class buttons, level setting and navigation remain visible. No class button was clicked in these static captures, so the changed callback message is source-reviewed rather than interactive acceptance.
